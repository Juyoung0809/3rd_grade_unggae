# 구현 이력

---

## Phase 1. 기반 설계

기존 구현된 상태로 인수인계됨 (dca6e9b 커밋 기준)

### 생성된 파일
- 프로젝트 전체 기반 구조 (Spring Boot, React + Vite)
- JWT 인증 구조, 공통 응답(`ApiResponse`), 예외 처리(`CustomException`, `GlobalExceptionHandler`)

---

## Phase 2. 인증/권한 (P2)

완료일: 2026-04-20

### 수정된 파일
- `User.java` — `name` → `nickname`, `bio` 추가, `profileImage` → `profileImageKey`
- `RegisterRequest.java`, `TokenResponse.java`, `AuthService.java` — nickname 반영
- `CourseResponse.java`, `EnrollmentResponse.java`, `PaymentResponse.java`, `PaymentHistoryResponse.java` — `getName()` → `getNickname()`
- `RatingResponse.java`, `AnswerResponse.java`, `QuestionResponse.java`, `QuestionSummaryResponse.java` — nickname 반영
- `AuthContext.tsx`, `NavBar.tsx`, `AuthPage.tsx`, `ProfilePage.tsx` — nickname 반영

### 생성된 파일
- `admin/dto/UserListResponse.java`
- `admin/dto/UserStatusUpdateRequest.java`
- `admin/dto/UserRoleUpdateRequest.java`
- `admin/service/AdminUserService.java`
- `admin/controller/AdminUserController.java` — `GET/PUT /api/admin/users/**`
- `Front/src/api/admin.ts` — 사용자 관리 API 함수
- `Front/src/pages/AdminPage.tsx` — 강의승인/회원관리 탭

### 설계 결정
- ERD 불일치(name→nickname, bio 필드 누락) 수정을 우선 진행
- ddl-auto: update 이므로 기존 `name` 컬럼이 DB에 잔존할 수 있음 → 개발 DB 초기화 권장

---

## Phase 3. 강의 CRUD + 섹션/레슨 (P3)

완료일: 2026-04-20

### 생성된 파일
- `section/entity/Section.java` — Course FK, title, orderNum
- `section/repository/SectionRepository.java`
- `section/dto/SectionCreateRequest.java`, `SectionUpdateRequest.java`, `SectionResponse.java`, `SectionOrderRequest.java`
- `section/service/SectionService.java`
- `section/controller/SectionController.java` — `GET/POST /api/courses/{courseId}/sections`, `PUT/DELETE /api/sections/{sectionId}`, `PATCH /api/courses/{courseId}/sections/order`
- `lecture/controller/SectionLectureController.java` — `GET/POST/PUT/DELETE/PATCH /api/sections/{sectionId}/lessons/**`
- `lecture/dto/LectureOrderRequest.java`
- `rating/controller/CourseReviewController.java` — `GET/POST/PUT/DELETE /api/courses/{courseId}/reviews/**`
- `Front/src/api/sections.ts` — 섹션/레슨 API

### 수정된 파일
- `lecture/entity/Lecture.java` — `section_id` nullable FK, `updateSection()`, `updateOrder()` 추가
- `lecture/dto/LectureResponse.java` — `sectionId` 필드 추가
- `lecture/repository/LectureRepository.java` — `findBySectionIdOrderByOrderIndexAsc`, `countBySectionId` 추가
- `lecture/service/LectureService.java` — 섹션 기반 레슨 CRUD 메서드 추가
- `course/controller/CourseController.java` — `POST/PUT/DELETE /api/courses` 추가, sort 파라미터 추가
- `course/controller/InstructorCourseController.java` — `GET /api/instructor/courses`만 유지
- `course/service/CourseService.java` — sort 파라미터 지원 (LATEST/RATING/STUDENTS)
- `course/repository/CourseRepository.java` — `findAllWithInstructor()` 추가
- `rating/service/RatingService.java` — `addRatingByCourse(userId, courseId, request)` 추가
- `admin/service/AdminCourseService.java` — `getAllCourses()` 추가
- `admin/controller/AdminCourseController.java` — `GET /api/admin/courses` 추가
- `common/response/ErrorCode.java` — `SECTION_NOT_FOUND`, `SECTION_FORBIDDEN` 추가
- `common/security/SecurityConfig.java` — PATCH 메서드 허용, `/api/sections/**` GET 공개
- `Front/src/api/instructor.ts` — POST/PUT/DELETE를 `/api/courses` 경로로 변경
- `Front/src/api/courses.ts` — sort 파라미터 추가
- `Front/src/api/ratings.ts` — `/api/courses/{courseId}/reviews` 새 경로 추가
- `Front/src/pages/CourseListPage.tsx` — 정렬 UI (최신순/평점순/수강생순) 추가
- `Front/src/pages/InstructorCoursesPage.tsx` — 섹션/레슨 관리 UI로 전면 재작성

### 설계 결정
- 기존 `/api/instructor/courses` POST/PUT/DELETE는 `/api/courses`로 경로 통합; 강사 목록 GET만 `/api/instructor/courses` 유지 (하위 호환)
- 기존 `/api/ratings` 엔드포인트는 유지하고, 신규 `/api/courses/{courseId}/reviews` 경로를 추가로 제공
- STUDENTS 정렬은 강의 수(lectureCount)로 대리 정렬 (실제 수강생 수 집계는 별도 쿼리 필요)

---

## Phase 6. 강사 수익 대시보드 (2026-05-09)

기획서·요구사항명세서에 없던 화면을 신규 설계 후 구현. `docs/09_강사수익대시보드_설계.md` 참조.

### 생성된 파일
- `enrollment/dto/MonthlyStatResponse.java` — 월별 수익 통계 DTO
- `enrollment/dto/InstructorRevenueSummaryResponse.java` — 수익 요약 DTO
- `enrollment/dto/InstructorSaleResponse.java` — 개별 판매 내역 DTO
- `enrollment/service/InstructorRevenueService.java` — 수익 집계 서비스
- `enrollment/controller/InstructorRevenueController.java` — `GET /api/instructor/revenue/summary`, `GET /api/instructor/revenue/sales`
- `Front/src/api/revenue.ts` — 수익 API 호출 모듈
- `Front/src/pages/InstructorRevenuePage.tsx` — 수익 대시보드 페이지 (`/instructor/revenue`)
- `docs/09_강사수익대시보드_설계.md` — 설계 문서

### 수정된 파일
- `enrollment/repository/EnrollmentRepository.java` — `findAllByInstructorId`, `findAllByInstructorIdAndCourseId` 쿼리 추가
- `Front/src/App.tsx` — `/instructor/revenue` 라우트 추가
- `Front/src/components/NavBar.tsx` — INSTRUCTOR 역할일 때 "수익 현황" 메뉴 추가 (데스크톱/모바일)

### 설계 결정
- Payment 엔티티는 Phase 6(결제모듈)이 비활성화 상태이므로 Enrollment를 직접 데이터 소스로 사용
- 취소(환불)된 수강은 수익 집계에서 제외하되, 판매 내역 목록에는 "취소됨" 상태로 표시
- 무료 강의(paidPrice=0)는 수익 0원으로 집계하나 판매 건수에는 포함

---

## Phase 5. 리뷰 UI (2026-05-07)

### 수정된 파일
- `Front/src/pages/CourseDetailPage.tsx` — 리뷰 API를 구 `/api/ratings` 에서 신 `/api/courses/{courseId}/reviews` 로 교체

### 설계 결정
- 기존 `/api/ratings` 엔드포인트는 유지하고, 신규 `/api/courses/{courseId}/reviews` 경로를 추가로 제공
