# 개발 체크리스트

> 05_개발계획서.md 기준

---

## Phase 1 — 기반 설계
- [x] DB 스키마 설계 및 MySQL 세팅
- [x] Spring Boot 프로젝트 초기화 (패키지 구조, 공통 응답, 예외 처리)
- [x] React + Vite 프로젝트 초기화 (라우터, Axios 인터셉터)
- [x] JWT 인증 구조 설계

## Phase 2 — 인증/사용자
- [x] 회원가입, 로그인, 로그아웃, Refresh Token 구현
- [x] 사용자 프로필 조회/수정
- [x] Spring Security 역할 기반 권한 처리
- [x] 로그인/회원가입 페이지 UI

## Phase 3 — 강의 CRUD
- [x] 강의 등록/수정/삭제 (강사)
- [x] 강의 목록/상세 조회 API (카테고리, 키워드, 정렬, 페이징)
- [x] 섹션 CRUD
- [x] 레슨(챕터) CRUD
- [x] 강의 목록/상세 페이지 UI
- [x] 강사 대시보드 UI

## Phase 4 — 수강/진도
- [x] 수강 신청 로직 (ENROLLMENT 생성)
- [x] 진도율 관리 (레슨 완료 처리)
- [x] 내 수강 목록 페이지

## Phase 5 — 리뷰(평점)
- [x] 리뷰(평점) CRUD
- [x] 평균 별점 자동 갱신
- [x] 리뷰 UI

## Phase 6 — 결제
- [x] 토스페이먼츠 결제 준비/확정 구현 (prepare → confirm)
- [x] 결제 서버 사이드 검증 로직 (Toss API 재확인)
- [x] 결제 완료 후 ENROLLMENT 자동 생성
- [x] 결제 페이지 (PaymentPage - 토스 JS SDK)
- [x] 결제 완료/실패 페이지 UI
- [x] 결제 내역 조회

## Phase 7 — 관리자
- [x] 강의 승인/반려 API
- [x] 회원 관리 API (상태/역할 변경)
- [x] 결제 취소(환불) API (실제 Toss cancel API 호출)
- [x] 관리자 대시보드 UI

## Phase P3 — 운영형 외부연동 + 관측성

### 스케쥴러
- [x] 만료된 RefreshToken 정기 삭제 (매일 새벽 2시)
- [x] 일별 결제 통계 로그 (매일 오전 9시)
- [x] PENDING 결제 만료 처리 (매 시간)

### Webhook
- [x] 토스페이먼츠 웹훅 수신 엔드포인트 (POST /api/webhooks/toss)

### 모니터링/관측성
- [x] Spring Boot Actuator (health, info, metrics, loggers, env)
- [x] MDC requestId 기반 운영 로그 필터

### 보안
- [x] XSS 방지 필터 (XssRequestWrapper로 Query Param/Header sanitize)
- [x] SQL Injection 방지 (JPA PreparedStatement 자동 처리)
- [x] 입력값 검증 (@Valid, @NotBlank, @Email 등)

### 외부연동
- [x] 토스페이먼츠 PG 연동 (결제 확정, 취소)

### 배포
- [x] Back Dockerfile (멀티스테이지 빌드)
- [x] Front Dockerfile + nginx.conf (SPA 라우팅, API 프록시)
- [x] docker-compose.yml (MySQL + Backend + Frontend)

## Phase P4 — 강의 목록 개선 + 미리보기 레슨 + 관리자 통계 + 홈페이지

### 강의 목록 (COURSE-06)
- [x] "수강생순" 정렬을 실제 활성 수강(Enrollment) 건수 기준으로 수정
- [x] GET /api/courses 페이징 처리 (page/size, 기본 page=0, size=12)
- [x] 강의 카드에 수강생 수 표시

### 미리보기 레슨 (LES-05)
- [x] Lecture에 `preview` 필드 추가 (강사가 미리보기 허용 여부 설정)
- [x] 강사 레슨 등록/수정 폼에 "미리보기 허용" 체크박스
- [x] 비수강생도 미리보기 레슨은 잠금 해제 및 재생 가능 (모달)

### 관리자 통계 대시보드 (ADMIN-04)
- [x] GET /api/admin/stats API (총 회원수/수강생/강사, 강의수, 수강건수, 결제건수, 매출)
- [x] 관리자 페이지 "통계" 탭 UI

### 홈페이지
- [x] HomePage(랜딩 페이지) 생성 — 히어로, 카테고리 바로가기, 인기 강의, CTA, 푸터
- [x] `/` 라우트 연결, NavBar "홈" 링크가 `/`로 이동
- [x] 카테고리 바로가기 → CourseListPage `?category=` 쿼리 연동

## Phase P5 — 토스페이먼츠 JS SDK 프론트엔드 실연동
- [x] `index.html`에 토스페이먼츠 v1 SDK 스크립트 추가
- [x] `window.TossPayments` 전역 타입 선언
- [x] PaymentPage에서 `preparePayment` → `TossPayments.requestPayment()` 실연동 (결제수단별 매핑)
- [x] PaymentSuccessPage에서 `confirmTossPayment` 호출로 결제 최종 확정
- [x] PaymentFailPage 재시도 흐름을 `/payment/:courseId`로 연결
- [x] `/payment/success`, `/payment/fail` 라우트 추가

## 요구사항 명세서 기준 충족 여부

| ID | 요구사항 | 구현 |
|----|---------|------|
| AUTH-01~07 | 인증 전체 | [x] |
| USER-01~06 | 사용자 관리 | [x] |
| COURSE-01~07 | 강의 CRUD | [x] |
| SEC-01~02, LES-01~05 | 섹션/레슨 | [x] |
| ENROLL-01~03, PROG-01~03 | 수강/진도 | [x] |
| REVIEW-01~05 | 리뷰(평점) | [x] |
| PAY-01~08 | 결제 | [x] |
| ADMIN-01~04 | 관리자 | [x] |
